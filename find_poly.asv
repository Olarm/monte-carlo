close all

I = 100;    % Number of monte-carlo simulations
N = 100;    % Number of values in y
K = 1000;   % Number of parameters in the prior

x = linspace(1,10, N);
R = random("Normal", 0, 2, [1, N]);

sq = random("Normal", 2,0.3);
y = x.^sq;

y_dist = zeros(1,20);
for i=1:20
    y_dist(i) = x.^(sq-1+i/10);
    
end

err = mean(

figure();
hold on
plot(y)
plot(y+R)
hold off

prior = random("Normal", 2, 1, [1, K]);
